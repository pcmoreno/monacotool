{% extends 'base.html.twig' %}

{% block title %}MonacoTool{% endblock %}

{% block body %}
<div class="min-h-full flex flex-col justify-center items-center px-6 py-24">
    <div class="text-center max-w-xl">
        <h1 class="text-5xl font-bold tracking-tight text-gray-900 mb-4">MonacoTool</h1>
        <p class="text-lg text-gray-500 mb-10">Monte Carlo forecasting for software teams. Predict delivery with confidence.</p>

        {% if app.user %}
            <p class="text-sm text-gray-500 mb-6">Logged in as <span class="font-medium text-gray-700">{{ app.user.name ?? app.user.email }}</span></p>
            <div class="flex justify-center gap-4">
                <a href="{{ path('app_team') }}" class="rounded-md bg-indigo-600 px-5 py-2.5 text-sm font-semibold text-white shadow hover:bg-indigo-500 transition">Go to teams</a>
                <a href="{{ path('app_logout') }}" class="rounded-md bg-white px-5 py-2.5 text-sm font-semibold text-gray-700 shadow ring-1 ring-gray-300 hover:bg-gray-50 transition">Logout</a>
            </div>
        {% else %}
            <a href="{{ path('app_login') }}" class="rounded-md bg-indigo-600 px-6 py-3 text-sm font-semibold text-white shadow hover:bg-indigo-500 transition">Login</a>
        {% endif %}
    </div>
</div>
{% endblock %}
