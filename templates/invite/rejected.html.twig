{% extends 'base.html.twig' %}

{% block title %}Invitation declined — MonacoTool{% endblock %}

{% block body %}
<div class="min-h-full flex flex-col justify-center items-center px-6 py-24">
    <div class="w-full max-w-sm text-center">
        <h1 class="text-2xl font-bold text-graphite-900 mb-2">MonacoTool</h1>
        <p class="text-graphite-500 text-sm mb-2">You've declined the invitation to join <strong class="text-graphite-700">{{ team.name }}</strong>.</p>
        <p class="text-graphite-400 text-sm">You can close this page.</p>
    </div>
</div>
{% endblock %}
